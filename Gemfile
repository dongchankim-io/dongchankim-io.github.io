source "https://rubygems.org"

# Lock to GitHub Pages’ dependency set so local == GitHub build
gem "github-pages", group: :jekyll_plugins

# Ruby 3 needs this for `jekyll serve`
gem "webrick", "~> 1.8"

# Explicitly include minima theme
gem "minima", "~> 2.5"
